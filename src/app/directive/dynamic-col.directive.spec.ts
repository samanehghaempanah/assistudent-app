import { DynamicColDirective } from './dynamic-col.directive';

describe('DynamicColDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicColDirective();
    expect(directive).toBeTruthy();
  });
});
