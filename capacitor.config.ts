import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'assistudent.core.app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
